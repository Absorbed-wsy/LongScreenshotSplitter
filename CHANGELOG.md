# 更新日志

## v0.2 · 2026-09-16

- 全新浅色双栏界面：分区设置、可滚动表单、固定操作栏，支持 Ctrl+O 选图。
- 新增异步长截图缩略预览、切分边界与重叠区域标记，参数修改后实时更新。
- 新增空状态、实时参数校验、动态命名示例，切分期间锁定设置，失败后可恢复重试。
- 超过 5000 万像素时跳过缩略图以节省内存；切片过多时抽样显示边界，避免界面卡顿。
- 修复快速切换至超大图片时预览与切分同时解码的竞态。
- 新增靛蓝软件图标，提供透明 PNG 和 16–256px 多尺寸 Windows ICO，并嵌入窗口及 EXE。
- 扩充到 23 项核心与 GUI 回归测试；打包自测新增图标、预览、编码器和失败恢复检查。
- 新增标签触发的 Windows 自动构建与 Release 发布，附带 SHA-256 校验及打包自测报告。

## v0.1

首次发布。

- 中文 tkinter 界面，Windows EXE 无终端窗口启动，无需安装 Python。
- 支持 PNG/JPG/JPEG/WEBP 静态图片，自选输出目录。
- 自定义切片高度，提供 2000/3000/4000/5000px 预设。
- 默认保留 100px 重叠，可调整或关闭；尾片保留剩余高度。
- 默认无损 PNG，也可选择原格式。
- 支持命名前缀与编号位数，显示原图尺寸、预计数量和首尾切片范围。
- 后台切分、进度提示、完成后打开文件夹及同名文件保护。
